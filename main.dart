
body:Container(
        decoration: BoxDecoration(
          gradient: LinearGradient(
            colors: [
              Color(0xffffecd2),Color(0xffcb69f)
            ],
            begin: FractionalOffset(1.0,0.5),
            end: FractionalOffset(0.0, 0.5),
              stops: [0.0,0.0,1.0]
          )
        )
      )
